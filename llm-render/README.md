# Small LLM API (Render Deployment)

## Features
- Runs Phi-3 Mini LLM
- FastAPI backend
- Hugging Face model download
- Deployable on Render free tier

## Endpoints
- GET / → status
- GET /generate?prompt=hello → AI response

## Deploy
Push to GitHub → Connect Render → Deploy as Docker service